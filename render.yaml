services:
  # Web Service — Backend API (Express.js)
  - type: web
    name: asset-management-api
    env: node
    region: singapore
    plan: starter
    buildCommand: npm install && npm run build --workspace=packages/shared && npm run build --workspace=apps/api
    # Schema changes must be applied through reviewed Prisma migrations, not on every startup.
    startCommand: cd apps/api && node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CORS_ORIGIN
        value: https://iam-api-sandy.vercel.app
      - key: DATABASE_URL
        fromDatabase:
          name: asset-management-db
          property: connectionString

databases:
  # PostgreSQL Database Managed
  - name: asset-management-db
    databaseName: asset_management
    user: asset_user
    plan: starter
    region: singapore
